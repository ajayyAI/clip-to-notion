{
  "name": "clip-to-notion",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension to save X posts into a Notion database.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
